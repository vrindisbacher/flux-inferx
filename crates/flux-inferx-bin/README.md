# flux-inferx-bin

Executable wrappers that run `rustc` and `cargo` with `flux-inferx`.

Much of the design is inspired by how
[`Prusti`](https://github.com/viperproject/prusti-dev/) set up its executables.
